<div id="cart-variant-details" data-getQrCode="{{ path('sylius_mollie_plugin_get_qr_code') }}"
     data-removeQrCode="{{ path('sylius_mollie_plugin_remove_qr_code') }}">
    {% for key, item in order.items %}
        <div data-index="{{ key }}" data-recurring="{{ (item.variant.recurring ? '1' : 0) | default('0') }}"{% if item.variant.recurring %} data-interval="{{ item.variant.interval }}" data-times="{{ item.variant.times }}"{% endif %}></div>
    {% endfor %}
</div>

{% include "@SyliusMolliePlugin/Shop/_partial/_recurringTranslations.html.twig" %}
